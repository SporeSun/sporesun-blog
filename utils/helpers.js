module.exports = {
    // Example helper function to format date
    formatDate: (date) => {
      return `${date.getDate()}/${date.getMonth() + 1}/${date.getFullYear()}`;
    },
  
    // Add more helper functions as needed
  };
  